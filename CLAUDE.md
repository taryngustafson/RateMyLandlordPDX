@AGENTS.md

## Long-running commands
- Always run `npm run dev`, docker-compose, any --watch command in background
- `npm install` may take up to 5 minutes
- `npm run build` may take up to 10 minutes
